inhibition_features.csv contains the featurized dataset generated with generate_features.ipynb

The y is column % control.
Protein HMS LINCS ID is the ID, and should be dropped before use of the dataset. The rest of the columns are the features.
